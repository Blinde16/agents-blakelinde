# Color and typography

Primary UI surfaces use near-black backgrounds with zinc neutrals and emerald accents for status and success. Typography: system sans for UI chrome; monospace for identifiers, thread IDs, and audit lines. Do not introduce rainbow gradients in product UI unless explicitly in a marketing-only asset outside the command center.
